<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.it_hacaton">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ithacaton">
        <activity android:name=".AddUserToForAdminActivity"></activity>
        <activity android:name=".AddDBForAdminActivity" />
        <activity android:name=".UsersInDBForAdminActivity" />
        <activity android:name=".CreateNewsForAdminActivity" />
        <activity android:name=".ListOfDBForAdminActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainForAdminActivity" />
    </application>

</manifest>